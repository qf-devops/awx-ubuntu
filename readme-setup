

root@awx:~# history
    1  clear
    2  sudo apt-get update && sudo apt-get install software-properties-common && sudo apt-add-repository ppa:ansible/ansible && sudo apt-get update && sudo apt-get install ansible
    3  sudo apt-get update && sudo apt-get install   apt-transport-https   ca-certificates   curl   software-properties-common && curl -fsSL https://download.docker.com/linux/ubuntu/gpg | sudo apt-key add - && sudo apt-key fingerprint 0EBFCD88 && sudo add-apt-repository   "deb [arch=amd64] https://download.docker.com/linux/ubuntu \
    4    $(lsb_release -cs) \
    5    stable" && sudo apt-get update && sudo apt-get install docker-ce
    6  sudo apt-get install python-pip
    7  pip install docker-py 
    8  sudo apt-get install python-pip &&  pip install docker-py 
    9  git clone https://github.com/ansible/awx.git
   10  which pip
   11  pip install docker-py 
   12  export LC_ALL="en_US.UTF-8" && export LC_CTYPE="en_US.UTF-8"
   13  pip install docker-py 
   14  cd awx/
   15  ls -lrt
   16  pwd
   17  cd installer/
   18  ls -lrt
   19  ansible-playbook -i inventory install.yml
   20  docker ps
   21  exit
   22  ssh-keygen
   23  cat .ssh/id_rsa.pub 
   24  ssh node01.example.com
   25  ssh 192.168.32.10
   26  cd
   27  cd .ssh/
   28  cat id_rsa.pub 
   29  cat id_rsa
   30  history
